sudo: required
services:
  - docker

script:
  - echo "Build xgo-base image"
  # - cd docker/base/
  - docker build -t "xgo-base" -f docker/base/Dockerfile .
  - docker build -t "xgo-1.7.1" -f docker/go-1.7.1/Dockerfile .
  - echo "List all image"
  - docker images
